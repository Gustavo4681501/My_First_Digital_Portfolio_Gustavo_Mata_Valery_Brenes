<h1>Editing project</h1>

<%= render "form_edit", project: @project %>

<br>

<div>
  <%= link_to "Show this project", edit_admin_project_path(@project) %> |
  <%= link_to "Back to projects", projects_path %>
  <%= link_to "Destroy this project", admin_project_path(@project), data: {
                                    turbo_method: :delete,
                                    turbo_confirm: "Are you sure?"
                              }%>
